import CLI
import Scheduler

class OS:
	def __init__(self):
		#self.cli = CLI.CLI()
		#self.scheduler = Scheduler.Scheduler()
		#self.cli.set_scheduler(self.scheduler)
		#self.scheduler.set_cli(self.cli)
		#self.cli.run()
		pass